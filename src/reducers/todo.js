export const initialState = {
    item: '',
    completed: false,
    id: Date.now()
}

export function reducer(state, action) {
    switch (action.type) {
        default:
            return state;
    }
}